# TODO / 進捗・パッケージ一覧

InstantMesh フェーズ1（Windows/macOS/Linux デスクトップ）の進捗と、`pkg/` 全パッケージの役割・状態を網羅する。**本ファイルが進捗・パッケージ一覧の正**。新規パッケージを足したらここに 1 行追加する（`README.md` のプロジェクト構成ツリーにも反映する）。

- 全体像・要件: `README.md` と `docs/`（要件は `docs/要件定義書.md`、アーキテクチャは `docs/システムアーキテクチャ定義書.md` が正）
- 開発ルール: `CLAUDE.md`「設計原則」節
- 合否判定は CI（`.github/workflows/ci.yml`）が正（`pkg/` 全パッケージ 100% カバレッジ強制・`govulncheck`・クロスビルド）

## 現在のステータス（サマリ）

- **コア（`pkg/`）**: 40 パッケージすべて実装済み・**ユニットテスト 100% カバレッジ**（CI 強制）。
- **サーバー（`cmd/server`）**: シグナリング（`/ws`）＋リレー（`/relay`）を単一プロセスで提供。制御プレーン（ルーム作成・待合室承認・キック・エフェメラル管理）とデータプレーン（リレー中継・通信量制限）が動作。Cognito JWT 認証・S3 監査はインターフェース実装済み（フラグ指定時に有効）。セッションはインメモリ（Redis 未使用）。
- **クライアント（`cmd/client`）**: 既定は **GUI モード**（LocalAPI ＋ 埋め込み SPA。Windows は WebView2、Linux は Chromium 系 `--app` によるアプリ内ウィンドウ、その他 OS（macOS 等）は既定ブラウザ）。ヘッドレス CLI（`-mode host`/`guest`）も提供。鍵生成・招待リンク（帯域外 MITM 照合）・STUN・wireguard-go 設定・仮想NICへのIP付与・P2P 直通の成否検知とリレーへの自動フォールバックまで動作。
- **実機検証**: 2 台の実機（ParrotOS×2・VMware NAT モード）でメッシュ疎通（仮想NIC生成・割当IP経由の `ping`）を確認済み。
- **共有層（§4.6 / §4.7）**: 2026-07-27 の方向転換で新設。**§4.6 の到達手段 3 経路（名前解決・メッシュIP 直接・loopback プロキシ）と §4.7 の統制はすべて実装済み**。以下は §4.6.1 / §4.6.3 の詳細——検出（`pkg/localsvc` ＋ TCP プローブ ＋ `GET /api/services`）、共有選択（`POST /api/share`）、名前の配布（`peer_info` に相乗り）、ローカル DNS レスポンダ（`pkg/dnsmsg` ＋ `pkg/meshname` ＋ 自メッシュIP:53 の UDP 待受）、OS への split DNS 注入（`dnsconfig_<os>.go`・起動時の残骸回収つき）。副の経路（§4.6.4）は `-loopback` で有効化する loopback プロキシ（`pkg/portmap` ＋ `cmd/client/loopback.go`）。**実機検証は全面的に未了**（特権・実 OS・実 NAT を要するため非特権 CI では検証できない。手順は `docs/実機検証チェックリスト.md`）。

## 残タスク

> **2026-07-27 方向転換**: 前面を「ローカルAI／MCP の一時貸与」へ寄せた（`docs/要件定義書.md` 付録C）。既存のメッシュ基盤（下記「土台」）は削らず、その上に共有層（下記「共有層」）を載せる。

> **実機検証の手順は `docs/実機検証チェックリスト.md` が正**。以下で「残: 実機検証」と書かれた項目は同書の V-xx に 1 対 1 で対応する（合格したら該当行から「残: 実機検証」を外し、同書の記録表へ日付・OS・結果を残す）。

### 土台（共有層の動作前提。ここが崩れていると共有層の不具合を切り分けられない）

- [ ] 独立した実 NAT 間での UDP 穴あけ（hole punching）確立の疎通検証（前回は同一 NAT 配下のため経路判定は未確認）。**要件 §7 の P2P 直通成功率 80% の達成判定を兼ねる。**
- [ ] 直通⇄リレー切替（フォールバック）の実疎通検証。
- [ ] AWS 本番展開（`docs/AWS展開設計書.md`）: S1-1 Cognito 認証・S1-2 S3 監査は実装済み。S1-3 IaC（AWS CDK）は**別リポジトリ**で管理。S1-4 本番導通確認は未着手。

### 共有層（方向転換で新規・要件 §4.6 / §4.7）

- [x] **名前解決（主・§4.6.3）**: 「**権威はホスト、解決はローカル**」。`pkg/signaling` の `PeerInfo` に `Names`/`Services` を追加して既存のシグナリング経路で写像を配布し、名前空間と写像は `pkg/meshname`（`Zone` はゴルーチンセーフ・名前は先着優先）、DNS メッセージの解析/応答は `pkg/dnsmsg`（`pkg/stun` と同じ分割）に置いた。`cmd/client` は自メッシュIP の `:53` で UDP レスポンダを駆動し、OS へ split DNS を注入する（`dnsconfig_<os>.go`: Windows=NRPT / macOS=`/etc/resolver/` / Linux=systemd-resolved）。起動時の残骸回収は `cleanupStaleSplitDNS`（`main` の冒頭で無条件実行）。`hosts` 書き換えとホスト側DNSサーバー方式は採用していない。
    - 残: **実機検証**（Windows/macOS/Linux での注入・解決・残骸回収。非特権 CI では検証できない）。
    - 既知の制約: 名前の配布は `peer_info` に相乗りするため、`-stun=` で STUN を無効化した場合は配布されない（メッシュIP 直接での到達は可能）。DNS over TCP は未対応（応答は 512 バイト未満に収まるため stub リゾルバは UDP で完結する）。
- [x] **ローカルサービス検出・共有の選択**（§4.6.1）: 既知ポート表（11434 / 1234 / 8000 / 8080 / 3000 / 80）の判定・候補の組み立ては `pkg/localsvc`（純粋）、プローブは `cmd/client/svcprobe.go` で **TCP connect まで**（HTTP は送らない）。走査先はループバック（`127.0.0.1` と `::1`）のみで、ホスト側サービスに `0.0.0.0` バインドを要求しない。GUI は `GET /api/services` で候補を取得し、ホスト画面にメッシュIP付き URL（§4.6.2 経路(2)）を提示する。
    - 共有の選択は GUI のチェックボックス → `POST /api/share`（`cmd/client/sharing.go` の `shareController` が保持し、ゾーン・表示・`peer_info` の再広告へ反映）。空配列を送れば共有を全て止められる。
- [x] **ホスト側フォワーダ（§4.6.2「前提」・付録C.9 D-10）**: メッシュIP:ポート → `localhost`:ポート のユーザ空間転送（`cmd/client/svcforward.go`）。共有の変更で差分適用し、外れたポートは直ちに解放する。サービスが既に `0.0.0.0` バインドで bind できない場合は転送せず直接到達させる（bind 可否がそのまま判別になる）。転送コア（`forwarder`）は待受と転送先を独立に受けるため、ゲスト側 loopback プロキシ（§4.6.4）へそのまま転用できる。
    - 残: **実機検証**（`127.0.0.1` バインドの Ollama へゲストから到達できること）。TCP のみで、UDP は名前解決／メッシュIP 直接の経路を使う。
- [x] **共有選択を到達制御にする（付録C.9 D-11）**: wireguard-go の `tun.Device` を包み（`cmd/client/tunfilter.go`）、受信パスで宛先ポートを検査して共有中のポート以外への**新規接続**を落とす。TCP は SYN のみ検査するため、ホスト側から張った接続の戻りは状態を持たずに通る。ICMP（疎通確認）と自メッシュIP の `:53`（名前解決）は共有の有無に関わらず通す。解析と可否判定は `pkg/pktfilter`（純粋・fail-closed）。ゲスト側も既定で「自分宛の新規接続はすべて落とす」（借りる側の端末を開かせない）。切り分け用に `-share-guard=false` で無効化できる。
    - 同じ地点でゲスト別・共有サービス別のバイトを計上する（`pkg/usage`）。**計上はホスト側クライアントのみ**（設計原則2）。閲覧は有料プラン限定（`plan.Spec.UsageRecords`・`GET /api/usage`）。
    - 残: **実機検証**（共有していないポートが遮断され、ping と名前解決は通ること）。L7（アクセスキー・リクエスト単位／モデル単位の記録）は付録C.9.1 の未決事項。
- [x] **クライアント設定の永続化（付録C.9 D-14）**: メッシュ名ラベルと共有選択をローカル設定（OS のユーザ設定ディレクトリの `InstantMesh/config.json`）へ保存し、GUI（`GET/POST /api/config`）から名前を編集できるようにした。設定の表現・正規化・符号化は `pkg/clientconf`（純粋・保存できるフィールドを 2 項目に限定して**秘密が混ざらないことを型で保証**）、保存場所の決定と原子的な書き込み（一時ファイル → rename・ディレクトリ 0700 / ファイル 0600）は `cmd/client/clientconfig.go`。**秘密鍵・招待トークン・アクセストークン・アクセスキーは従来どおり保存しない**（設計原則3）。
    - ラベルの優先順位は `-mesh-name` > 保存済み設定 > OS のホスト名 > `host`。保存の無効化は `-config=`（空文字）。自身より新しい形式の設定は読み込みを拒否し、**上書き保存も止める**（新しいクライアントの設定を壊さない）。
    - セッション終了時の初期化（`shareController.reset`）は IP・接続・発行済みキー・プランを捨てる一方、**メッシュ名と共有の選択は持ち越す**（§4.6.2 の「セッションをまたいで名前が安定する」）。共有の選択の復元は GUI モードのみ（ヘッドレス CLI は選択を変える導線が無いため、貸し出しを固定化させない）。
    - 残: **実機検証**（V-16。保存内容に秘密が無いこと・再起動後も同じ名前と選択で到達できること）。
- [x] **loopback プロキシ（副・§4.6.4）**: OS の DNS を触れない環境向けの代替手段。`-loopback` で明示的に有効化する（**既定 false**・要 `-tunnel`）。ゲスト側は `127.0.0.1` 限定 bind で **TCP のみ**。ポート写像は `pkg/portmap`（純粋）——元ポートが空いていれば保存し、埋まっていれば `20000 + SHA256(ホスト公開鍵 ‖ 元ポート) mod 10000` へ**決定的に退避**し、そこも埋まっていれば導出範囲内を線形探索する（ランダム割当は採らない）。待受の駆動は `cmd/client/loopback.go`（`loopbackProxy`）で、転送コアはホスト側フォワーダと同一（`newForwarder`）。
    - 実際の待受ポートは表示状態（`appstate.SharedService.Local` → `SharedView.LocalURL`/`LocalMoved`）へ載せ、GUI が退避を明示してコピーさせる。待受を確保できなかったサービスは表示から消さず、`LocalURL` を空にして「この経路だけ使えない」ことが分かる形で残す。
    - 即時解放: ホストの広告変化（共有停止）で差分適用、キック・解散・退出・時間切れ・プロセス終了は ctx キャンセルで全解放。継続中の待受は張り替えない（確立済み接続を切らない）。**この待受管理はホスト側フォワーダと共通**（`cmd/client/listenerset.go` の `listenerSet`）で、両者が与えるのは「待受をどう開くか」だけ——§4.6.4 が要求する即時解放の不変条件を 1 箇所に閉じる。
    - 空きの判定は**実際の bind 試行**で行う（確認してから bind し直す二段構えは、その間に他プロセスへ取られうる）。
    - 残: **実機検証**（V-17。ポート保存・衝突時の決定的な退避・即時解放）。
    - 既知の制約: TCP のみ（UDP は名前解決／メッシュIP 直接の経路を使う）。既定が false なのは、元ポートの占有がゲスト自身の同種サービス起動を妨げるため（付録C.7 で主従を入れ替えた理由そのもの）。
- [ ] **GUI 第一画面の組み替え**（§3.1）: 主導線を「共有するローカルAIを選ぶ」へ。汎用ルーム作成は副導線として維持。`pkg/appstate` の共有中サービス表示（`SharedService`・到達 URL の組み立て）は実装済みで、残るのは**第一画面（idle 画面）の導線そのものの組み替え**。
- [ ] **利用記録**（§4.7）: L4 のバイト計上（ゲスト別・共有サービス別）と Pro 限定の閲覧は実装済み（`pkg/usage`・`GET /api/usage`）。リクエスト数の計上は L7 ゲート経由で実装済み。**残**: 記録の永続化の要否、**モデル単位**の識別へ踏み込むかの判断（付録C.9.1・D-16 で当面は見送り）。
- [x] **ゲストごとのアクセスキー・上限**（§4.7・有料機能・付録C.9 D-16）: 共有サービスの手前に HTTP プロキシ（`cmd/client/l7gate.go`）を置き、`Authorization: Bearer`／`X-InstantMesh-Key` のキーを検証、ゲスト単位の上限超過は当該ゲストのみ 429。キーは `pkg/accesskey`（メモリ内のみ・定数時間比較・再発行で旧キー即時失効・キックとは独立）。上限は `pkg/usage`。GUI は `GET/POST /api/control`。
    - 残: **実機検証**（Ollama / OpenAI 互換クライアントの API キー欄で通ること、ストリーミング応答が途切れないこと）。TLS 終端・非 HTTP サービスは対象外（キー要求を無効にして L4 の到達制御のみで運用する）。
- [x] **`pkg/plan` の拡張**（§5）: 統制機能のフラグを `Spec` に追加済み（`UsageRecords` / `AccessKeys` / `GuestLimits`・いずれも Pro のみ）。**同時共有サービス数は実装済み**（`MaxSharedServices`・Free=3 / Pro=10・付録C.9 D-15）。**既存の量的数値（ゲスト数・制限時間・リレー上限）は変更しない** — 緩和案は実測前のため保留（付録C.8）。§5 の表と `pkg/plan` は常に一致させる。

### 保留・後続

- [ ] MCP のツール呼び出し単位での承認・記録（プロトコル解釈層が必要。フェーズ1スコープ外）。
- [ ] ワンタイムパス: ティア判定をルーム作成時に確定しルームへ焼き付ける設計変更が前提（現行 `pkg/plan.TierForGroups` は期限の概念を持たない）。
- [ ] 水平スケール（ステージ2）: `pkg/manager`・`hub`・`relayhub` のプロセスローカル状態を Redis 等へ差し替え（将来。インターフェースは温存済み）。

## `pkg/` パッケージ一覧（全 40・すべて 100% カバレッジ）

### ドメイン基礎

| パッケージ | 役割 | 状態 |
| :--- | :--- | :--- |
| `plan` | プラン別仕様（Free/Pro のゲスト数上限・制限時間・リレー通信量）の一本化 | ✅ |
| `nickname` | ゲスト表示名（自己申告・未検証）の正規化と検証 | ✅ |
| `token` | 招待トークン生成・公開鍵の短縮フィンガープリント（SAS）生成 | ✅ |
| `ipam` | ルーム内の仮想IPアドレス割当（/24 内の払い出し・回収） | ✅ |
| `ratelimit` | キー単位のトークンバケット型レート制限 | ✅ |
| `clientip` | 信頼プロキシ設定に基づく HTTP リクエストの実クライアント IP 解決 | ✅ |

### ルーム集約・管理

| パッケージ | 役割 | 状態 |
| :--- | :--- | :--- |
| `room` | ルーム（一時的な仮想ネットワーク）の集約ルート（待合室承認/拒否/失効/キック/ライフサイクル） | ✅ |
| `manager` | 複数ルームのゴルーチンセーフな管理（ID/トークン索引・/24 払出・掃除） | ✅ |

### 制御プレーン（シグナリング）

| パッケージ | 役割 | 状態 |
| :--- | :--- | :--- |
| `signaling` | コントロールプレーンのメッセージスキーマ（Envelope のエンコード/デコード/検証） | ✅ |
| `session` | 純粋ディスパッチャ（メッセージ→ドメイン操作→宛先付き応答。I/O なし） | ✅ |
| `hub` | トランスポート非依存のサーバーコア（接続レジストリと配線） | ✅ |
| `cognitojwt` | Amazon Cognito 発行 JWT（ID トークン）の検証（純粋ロジック・now 注入） | ✅ |
| `auditlog` | 接続メタデータ監査ログの純粋ロジック（イベント表現・バッチバッファ・シリアライズ） | ✅ |

### データプレーン（リレー）

| パッケージ | 役割 | 状態 |
| :--- | :--- | :--- |
| `relay` | リレー中継の通信量メータリングと速度制限（スロットル） | ✅ |
| `relayhub` | 宛先公開鍵ベースの暗号化パケット中継（DERP 相当） | ✅ |
| `relayframe` | リレー（データプレーン）のワイヤフレーム形式（Encode/Decode） | ✅ |

### NAT トラバーサル

| パッケージ | 役割 | 状態 |
| :--- | :--- | :--- |
| `stun` | 自身の WAN 側マッピング発見のための STUN メッセージ生成/解析 | ✅ |
| `stunmux` | WireGuard の UDP ソケット（conn.Bind）に相乗りして STUN を行う | ✅ |
| `wgstat` | wireguard-go UAPI get 出力（`last_handshake_time` 等）の解析 | ✅ |
| `connmon` | ピアごとの接続性（直通/リレー）状態機械（now 注入） | ✅ |

### クライアント基盤

| パッケージ | 役割 | 状態 |
| :--- | :--- | :--- |
| `wgkey` | WireGuard 互換鍵ペア（Curve25519 / X25519）の生成・符号化 | ✅ |
| `secret` | 秘密鍵など機微なバイト列のメモリ内安全保持（使用後ゼロ化） | ✅ |
| `invite` | 招待リンク / QR ペイロードの生成・解析・検証（ホスト公開鍵埋め込み） | ✅ |
| `qr` | 文字列を QR コード（Model 2）のモジュール行列へ符号化 | ✅ |
| `signalclient` | シグナリングサーバーと会話するクライアント側ライブラリ | ✅ |
| `wsconn` | `gorilla/websocket` を `signalclient.Conn` へ適合させるアダプタ | ✅ |
| `wgconf` | wireguard-go 設定用の UAPI 設定文字列ビルダー | ✅ |
| `meshpeer` | シグナリングで得たピア情報（公開鍵・メッシュIP・WANエンドポイント）の写像 | ✅ |
| `netcfg` | 割当メッシュIPから仮想NICに適用するアドレス設定・ルートの算出 | ✅ |
| `appstate` | クライアント GUI のビューモデル（画面遷移・表示データ）を保持 | ✅ |
| `originguard` | GUI LocalAPI の同一オリジン検証（CSRF・DNS リバインディング対策） | ✅ |
| `oauthpkce` | OAuth 2.0 Authorization Code + PKCE フロー（RFC 7636）の純粋ロジック | ✅ |
| `clientconf` | クライアントのローカル設定（メッシュ名ラベル・共有の選択）の表現・正規化と保存形式の符号化（保存場所とファイル I/O は持たない・秘密は含めない） | ✅ |

### 共有層（ローカルサービス共有・§4.6）

| パッケージ | 役割 | 状態 |
| :--- | :--- | :--- |
| `localsvc` | 共有候補となるローカルサービスの既知ポート表（表示ラベル・名前解決用スラッグ）と、候補一覧・共有広告の組み立て | ✅ |
| `meshname` | `.mesh` 名前空間の定義（ラベル規則・FQDN 組み立て）と、名前 ⇄ メッシュIP の写像（ゴルーチンセーフな `Zone`） | ✅ |
| `dnsmsg` | DNS メッセージ（RFC 1035）の解析と権威応答の組み立て（ソケットは持たない・`pkg/stun` と同じ分割） | ✅ |
| `pktfilter` | IP パケットの最小解析と、共有していない宛先への到達を落とす可否判定（fail-closed） | ✅ |
| `accesskey` | ゲストごとのアクセスキーの発行・検証（定数時間比較）・失効（キックとは独立） | ✅ |
| `usage` | 共有サービスの利用記録（ゲスト別・サービス別の通信量。now 注入・通信内容は扱わない） | ✅ |
| `portmap` | ゲスト側 loopback プロキシのポート写像（元ポートの保存と、衝突時の決定的な代替ポート導出・線形探索） | ✅ |

> `cmd/server`・`cmd/client` は WebSocket・wireguard-go・OS・認証などを純粋コアへ配線する I/O アダプタ層のため、100% カバレッジ対象外（CI では `pkg/` のみ 100% 強制）。
